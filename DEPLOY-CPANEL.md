# انتشار هادس بورد روی هاست PHP

1. در پنل هاست مطمئن شوید ایمیل `info@hadesboard.com` ساخته و فعال است.
2. دستور `npm run package:cpanel` را اجرا کنید. این دستور سایت را می‌سازد، فایل `hadesboard-cpanel.zip` را به‌روز می‌کند و سالم‌بودن آرشیو را بررسی می‌کند.
3. فایل `hadesboard-cpanel.zip` را داخل پوشه وب دامنه (معمولاً `public_html`) بارگذاری و همان‌جا Extract کنید.
4. اگر فایل‌های مخفی نمایش داده نمی‌شوند، نمایش آن‌ها را در File Manager فعال کنید تا `.htaccess` هم منتقل شود.
5. فرم تماس را یک‌بار با اطلاعات واقعی آزمایش کنید و پوشه Spam ایمیل مقصد را نیز بررسی کنید.

فرم با تابع `mail()` خود PHP ارسال می‌شود: فرستنده `info@hadesboard.com` و گیرنده `m.gandomi1995@gmail.com` است. برای تحویل بهتر ایمیل، SPF و DKIM دامنه را در بخش تنظیمات ایمیل هاست فعال کنید.
